name: blog_platform_test
description: Test package for fenv_graphql_codegen integration tests
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  # Core dependencies would go here in a real project
  # For now, keeping minimal for test purposes

dev_dependencies:
  build_runner: ^2.10.0
  fenv_graphql_codegen:
    path: ../../../../  # Points to the actual codegen package
  test: ^1.24.0
